


export const SET_ATTRIBUTE_VALUE = 1;
export const SET_ATTRIBUTE = 2;
export const SET_CONTENT = 3;


export function parseAction(s:string){
    const lastChar = s[s.length - 1];
    if(lastChar === ':')
    for(let i = 0; i < s.length; i++){

    }
}